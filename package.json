{
  "name": "swarm-channel-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-agent swarm communication for Claude Code",
  "scripts": {
    "build": "npm run build --prefix service && npm run build --prefix plugin",
    "build:ui": "npm run build --prefix ui",
    "start:service": "npm start --prefix service",
    "start:ui": "npm run dev --prefix ui",
    "install:all": "npm install --prefix service && npm install --prefix plugin && npm install --prefix ui"
  }
}
